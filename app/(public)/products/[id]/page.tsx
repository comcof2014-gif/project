import { canViewBusinessPrice, getMockSession } from '@/lib/auth'; import { products } from '@/lib/mock-data'; import Link from 'next/link';
export default async function P({params}:{params:{id:string}}){const u=await getMockSession(); const p=products.find(x=>x.id===params.id); if(!p)return <div>상품 없음</div>; const b=canViewBusinessPrice(u); return <div className='space-y-3'><h1 className='text-2xl font-bold'>{p.name}</h1><p>일반가 {p.publicPrice.toLocaleString()}원</p>{b?<p>도매가 {p.businessPrice.toLocaleString()}원</p>:<p>설비업체 인증 후 도매가를 확인할 수 있습니다</p>}<a href={p.smartstoreUrl} className='block p-3 bg-green-700 text-white rounded text-center'>스마트스토어에서 구매하기</a><Link href='/installation/request' className='block p-3 bg-slate-900 text-white rounded text-center'>무료 설치중개 신청</Link><Link href='/quote/bulk' className='block p-3 bg-white border rounded text-center'>대량 견적 문의</Link></div>}
