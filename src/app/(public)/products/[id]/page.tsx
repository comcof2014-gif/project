import {products} from '@/lib/mock'; import Link from 'next/link'; export default async function P({params}:{params:Promise<{id:string}>}){const {id}=await params; const p=products.find(x=>x.id===id); if(!p) return <div>상품이 없습니다.</div>; return <div className='space-y-3'><h1 className='text-2xl font-bold'>{p.name}</h1><p>일반가 {p.publicPrice.toLocaleString()}원</p><p className='text-sm text-slate-600'>도매가는 승인된 회원만 표시됩니다.</p><div className='flex gap-2'><a href={p.smartstoreUrl} className='btn'>스마트스토어에서 구매하기</a><Link href='/installation/request' className='btn-secondary'>무료 설치중개 신청</Link><Link href='/quote/bulk' className='btn-secondary'>대량 견적 문의</Link></div></div>}